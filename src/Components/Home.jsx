import React from 'react';
import Feed from './Feed/Feed';
const Home = () => {
  return (
    <section>
      <Feed className="container mainContainer" />
    </section>
  );
};

export default Home;
